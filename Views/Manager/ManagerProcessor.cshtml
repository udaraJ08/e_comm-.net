@model List<ProcessorModal>

<div class="w-100 container" style="height: 100vh">

    <h1 class="f-bangers pt-5">Manage Processor</h1>

    <div class="form-container mt-5">
        <div class="row pt-3">
            <div class="form-group col-lg-4">
                <label class="control-label mb-2">Processor Title</label>
                <input id="processorTitle" class="form-control" />
                <span class="text-danger"></span>
            </div>
            <div class="form-group col-lg-4">
                <label class="control-label mb-2">Clock Speed</label>
                <input id="processorClockSpeed" class="form-control" />
                <span class="text-danger"></span>
            </div>
            <div class="form-group col-lg-4">
                <label class="control-label mb-2">Price (lkr)</label>
                <input type="number" id="processorPrice" class="form-control" />
                <span class="text-danger"></span>
            </div>
            <div class="d-flex justify-content-end mt-3">
                <div class="form-group">
                    <button class="btn btn-primary" onclick="addNewProcessor()">ADD NEW PROCESSOR</button>
                </div>
            </div>
        </div>
    </div>

    <div class="form-container mt-4">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Title</th>
                    <th scope="col">Clock Speed</th>
                    <th scope="col">Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var category in Model)
                {
                    <tr>
                        <th scope="row">@category.Id</th>
                        <td>@category.Title</td>
                        <td>@category.Clock_speed</td>
                        <td>Rs.@category.Price /=</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>